language: cpp
sudo: required
dist: trusty
compiler:
  - gcc

os:
  - linux
  
before_script:
  - mkdir build
  - cd build
  - cmake ../src/hello
  - cd ..

script:
  - cd build
  - make
  - ./hello
